---
phase: 04-output-and-polish
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified: []
autonomous: false
requirements: [TRANS-05, OUT-02, OUT-03, OUT-04]

must_haves:
  truths:
    - "User can see the original recipe and the adapted recipe side by side after transformation"
    - "Each substituted ingredient shows an Info icon that opens a popover with rationale on click"
    - "User can print the adapted recipe cleanly with no nav, form, or original column in the output"
    - "User can export the adapted recipe as an HTML file to Google Drive via OAuth2"
  artifacts:
    - path: "frontend/src/components/ComparisonLayout.tsx"
      provides: "Two-column layout at md: breakpoint"
      contains: "max-w-5xl"
    - path: "frontend/src/components/SubstitutionPopover.tsx"
      provides: "Click-to-open substitution rationale"
      contains: "PopoverContent"
    - path: "frontend/src/components/ExportToolbar.tsx"
      provides: "Print + Drive export buttons"
      contains: "window.print"
    - path: "frontend/.env.example"
      provides: "VITE_GOOGLE_CLIENT_ID documentation"
      contains: "VITE_GOOGLE_CLIENT_ID"
  key_links:
    - from: "Browser print dialog"
      to: "Adapted recipe card only"
      via: "print:hidden on form, toolbar, original column"
      pattern: "print:hidden"
---

<objective>
Human verification of all four Phase 4 success criteria. Claude cannot verify visual layout, print dialog output, or OAuth popup behavior — this checkpoint captures those four gates.

Purpose: Confirms that all four Phase 4 requirements (TRANS-05, OUT-02, OUT-03, OUT-04) are working end-to-end before marking the phase complete.

Output: Human approval or issue report. No files modified.
</objective>

<execution_context>
@/Users/mariabodoh/home/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariabodoh/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mariabodoh/repos/DietCode/.planning/ROADMAP.md
@/Users/mariabodoh/repos/DietCode/.planning/phases/04-output-and-polish/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 4 success criteria in the browser</name>
  <what-built>
    - Backend extended with originalIngredients, originalInstructions, and substitutionNote LLM prompt (Plan 01)
    - shadcn Popover installed, ComparisonLayout, RecipeCard className prop, print CSS (Plan 02)
    - SubstitutionPopover component, ExportToolbar with Print + Drive OAuth, App.tsx wired (Plan 03)
  </what-built>
  <how-to-verify>
Start the backend and frontend:

```bash
# Terminal 1
cd /Users/mariabodoh/repos/DietCode/backend && ./gradlew bootRun

# Terminal 2
cd /Users/mariabodoh/repos/DietCode/frontend && npm run dev
```

Visit http://localhost:5173

**Criterion 1 — Before/after comparison (OUT-02):**
1. Paste a recipe with at least 3 ingredients (e.g., a simple pasta recipe)
2. Select a dietary constraint (e.g., "Vegan" or "Gluten-free")
3. Click "Transform Recipe"
4. Verify: result shows TWO columns — "Original" on the left, "Adapted" on the right
5. Verify: both columns are visible at desktop width (>= 768px)
6. Resize browser to mobile width — verify columns stack vertically

**Criterion 2 — Substitution popovers (TRANS-05):**
1. On the adapted column, look for small ⓘ icons after ingredient names
2. Verify: icons appear only next to substituted/changed ingredients, NOT next to unchanged ones
3. Click an ⓘ icon — verify a popover opens with substitution rationale text
4. Press Escape — verify popover closes
5. Click outside the popover — verify popover closes

**Criterion 3 — Print export (OUT-03):**
1. Click "Print Recipe" in the toolbar
2. Verify: browser print dialog opens
3. In the print preview, verify:
   - The form and input section are NOT visible
   - The "Original" column is NOT visible
   - The "Adapted" recipe card IS visible with ingredients and instructions
   - No toolbar or "Edit / start over" button is visible
4. Close the print dialog (no need to actually print)

**Criterion 4 — Google Drive export (OUT-04):**
Only run if VITE_GOOGLE_CLIENT_ID is set in `frontend/.env.local`:
1. Add `VITE_GOOGLE_CLIENT_ID=your-client-id` to `frontend/.env.local`
2. Restart the dev server
3. Click "Export to Drive"
4. Verify: Google OAuth popup opens, label changes to "Connecting..."
5. Authenticate and grant the drive.file permission
6. Verify: label changes to "Exporting..." then returns to "Export to Drive"
7. Open Google Drive — verify a file named `{RecipeName}.html` (or with diet tags) appears

If Drive cannot be tested (no client ID), confirm the Drive button is disabled and the Print path (Criterion 3) works correctly. This is an acceptable fallback per D-09.
  </how-to-verify>
  <resume-signal>
Type "approved" if all verifiable criteria pass, or describe any issues found (e.g., "popover not appearing on substituted ingredients", "print preview shows form").

For Drive: type "drive-skipped" if VITE_GOOGLE_CLIENT_ID is not yet configured — criteria 1-3 passing is sufficient to unblock Phase 5.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification | No code changes in this plan — verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | — | Verification plan | accept | No new attack surface introduced; this plan contains no code changes |
</threat_model>

<verification>
Human approval via checkpoint above.

Phase 4 is complete when the user types "approved" or "drive-skipped" and confirms criteria 1-3 pass.
</verification>

<success_criteria>
1. User confirms: two-column comparison view appears after transformation with "Original" and "Adapted" columns.
2. User confirms: substitution popover opens on click, shows rationale, closes on Escape/outside click.
3. User confirms: print preview shows adapted recipe only (no form, no original column, no toolbar).
4. User confirms: Drive export triggers OAuth popup and uploads file, OR confirms Drive button is disabled when VITE_GOOGLE_CLIENT_ID is unset (acceptable fallback).
</success_criteria>

<output>
After completion, create `/Users/mariabodoh/repos/DietCode/.planning/phases/04-output-and-polish/04-04-SUMMARY.md`
</output>
