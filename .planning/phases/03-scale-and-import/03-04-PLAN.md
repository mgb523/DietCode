---
phase: 03-scale-and-import
plan: 04
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false
requirements:
  - TRANS-03
  - TRANS-04
  - INP-02
  - INP-03

must_haves:
  truths:
    - "User can set target servings in the form and the result recipe reflects scaled quantities"
    - "Salt and baking powder quantities at 2x scale are visibly less than double (sub-linear scaling verified by eye)"
    - "Pasting a URL in the recipe input shows the URL detected badge"
    - "A valid recipe URL (with JSON-LD) is successfully imported and transformed"
    - "An invalid or bot-blocked URL shows the scraping error message with the URL still in the input field"
    - "The result card stepper adjusts displayed quantities without triggering a new page load or network call"
  artifacts:
    - path: "backend/src/main/kotlin/com/dietcode/service/ScalingService.kt"
      provides: "No TODO() remains"
      contains: "fun scale"
    - path: "frontend/src/App.tsx"
      provides: "targetServings in fetch body"
      contains: "targetServings"
    - path: "frontend/src/components/RecipeCard.tsx"
      provides: "Client-side rescaling"
      contains: "rescaleIngredients"
  key_links: []
---

<objective>
Human verification checkpoint: confirm all five Phase 3 success criteria are met in the browser with a live backend.

Purpose: Plans 03-01 through 03-03 implement all Phase 3 functionality. This checkpoint ensures the integrated system works end-to-end before the phase is marked complete.

Output: Confirmed or documented failure for each Phase 3 success criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mariabodoh/repos/DietCode/.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 3 backend and frontend features:
    - ScalingService with sub-linear math (square root curve for leavening/salt/spices)
    - RecipeIngestionService URL scraping with JSON-LD → microdata → heuristic HTML fallback chain
    - TransformedRecipe.originalServings field for "(original: N)" display
    - TransformRequest.targetServings field sent from form
    - RecipeController wired to ScalingService
    - ServingStepper component (form + result card)
    - UrlDetectionBadge component
    - RecipeCard client-side rescaling with fraction.js
  </what-built>
  <how-to-verify>
    Start both services first:
    ```bash
    # Terminal 1 — backend
    cd /Users/mariabodoh/repos/DietCode/backend && ANTHROPIC_API_KEY=<your-key> ./gradlew bootRun

    # Terminal 2 — frontend
    cd /Users/mariabodoh/repos/DietCode/frontend && npm run dev
    ```
    Open http://localhost:5173

    **Check 1 — Target servings form field (Phase 3 criterion 1)**
    - The form should show a "Servings" label with [−] [2] [+] stepper below the dietary controls
    - Change servings to 4, paste a simple recipe (e.g., "1 tsp salt, 2 cups flour, mix and bake"), select no dietary constraints, submit
    - In the result card: ingredient quantities should be scaled — flour should show ~2x, salt should show less than 2x (sub-linear)
    - Confirm: result card shows a serving stepper

    **Check 2 — Sub-linear scaling visible at 2x (Phase 3 criterion 2)**
    - Use a recipe with "1 tsp baking powder" and "2 cups flour"
    - Submit with targetServings = 4 (assuming recipe serves 2)
    - In the result card: flour should be ~"4 cups" (2x), baking powder should be ~"1.41 tsp" or similar — NOT "2 tsp"
    - The sub-linear difference must be visible (baking powder clearly less than double)

    **Check 3 — URL detection badge (Phase 3 criterion 3)**
    - Type "https://www.example.com" in the recipe input field
    - A small "URL detected" badge with a link icon should appear in the top-right corner of the textarea
    - Delete the URL text → badge should disappear immediately

    **Check 4 — URL import from a JSON-LD recipe site (Phase 3 criterion 4)**
    - Paste a recipe URL from a food blog known to use JSON-LD (e.g., https://www.seriouseats.com/best-no-knead-bread-recipe or https://minimalistbaker.com/easy-overnight-oats/)
    - Submit with no dietary constraints
    - If the site serves the request: a transformed recipe should appear with ingredients
    - If the site blocks (403/bot detection): proceed to Check 5 instead — this is expected behavior for some sites

    **Check 5 — Scraping failure error (Phase 3 criterion 5)**
    - Enter an invalid URL: "https://localhost:9999/no-recipe"
    - Submit
    - Expect: error message "⚠ Couldn't import this URL. Open it, copy the recipe text, and paste it here."
    - Confirm: the URL is still visible in the input field (not cleared)
    - Confirm: form is still visible (not collapsed to result card)

    **Check 6 — Result card stepper rescaling (Phase 3 criterion 1 — post-result)**
    - After a successful transformation, use the [+] button in the result card to increase servings
    - Watch ingredient quantities update: linear ingredients should scale proportionally, salt/spices less so
    - Confirm: no network request fires (check browser DevTools Network tab — no fetch to localhost:8080)

    Report each check as PASS or FAIL with observations.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 Phase 3 success criteria pass.
    Type "issues: [description]" if any check fails — describe which criterion and what you observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan is verification-only; no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | N/A | Verification checkpoint | accept | No attack surface introduced by a human verification step |
</threat_model>

<verification>
All verification is performed manually by the developer per the how-to-verify steps above.

Automated pre-check (run before human verification):
```bash
cd /Users/mariabodoh/repos/DietCode/backend && ./gradlew test 2>&1 | tail -5
```
Must show BUILD SUCCESSFUL before starting manual verification.

```bash
cd /Users/mariabodoh/repos/DietCode/frontend && npm run build 2>&1 | tail -5
```
Must show BUILD SUCCESSFUL before starting dev server.
</verification>

<success_criteria>
Developer approves all five Phase 3 roadmap success criteria:
1. User can set a target number of servings and ingredient quantities update without a new LLM call
2. Leavening agents, salt, and strong spices are scaled sub-linearly (visibly different from linear at 2x)
3. Pasting a URL shows auto-detection badge and routes to the scraping path
4. A valid recipe URL with JSON-LD is ingested and transformed successfully (or correctly fails with error)
5. An invalid/bot-blocked URL shows the D-07 error message with URL preserved in the input field
</success_criteria>

<output>
After completion, create `/Users/mariabodoh/repos/DietCode/.planning/phases/03-scale-and-import/03-04-SUMMARY.md`
</output>
