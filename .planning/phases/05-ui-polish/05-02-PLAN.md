---
phase: 05-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/assets/dietcode-logo.svg
  - frontend/src/App.tsx
autonomous: true
requirements: [UI-POLISH-02]

must_haves:
  truths:
    - "A colored header band spanning full viewport width appears at the top of the page, above the recipe form"
    - "The DietCode logo SVG (kale leaf + fruit + robot) renders in the header at 36×36px, immediately left of the wordmark"
    - "The wordmark 'DietCode' is displayed in white text inside the header band (not the previous centered h1 on white)"
  artifacts:
    - path: "frontend/src/assets/dietcode-logo.svg"
      provides: "Playful SVG logo: kale leaf, colorful fruit, robot face"
      contains: "viewBox=\"0 0 40 40\""
    - path: "frontend/src/App.tsx"
      provides: "Colored <header> band before <main>, logo import"
      contains: "dietcode-logo.svg"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/assets/dietcode-logo.svg"
      via: "import dietcodeLogo from @/assets/dietcode-logo.svg"
      pattern: "dietcode-logo"
---

<objective>
Create the DietCode logo SVG and add the colored header band to App.tsx. This is the single biggest visual change in Phase 5: the page goes from all-white to having a warm emerald header that immediately communicates brand. The logo (kale leaf + fruit + robot, playful cartoon style) sits immediately left of the "DietCode" wordmark inside the header band.

Purpose: Visual identity — the page feels alive and branded immediately on load. D-02, D-04, D-05 from CONTEXT.md.

Output: New SVG asset file + updated App.tsx with header band structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mariabodoh/repos/DietCode/.planning/ROADMAP.md
@/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-CONTEXT.md
@/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-PATTERNS.md

<interfaces>
<!-- App.tsx current imports block (lines 1–9): -->
```tsx
import { useState } from "react"
import { RecipeCard } from "@/components/RecipeCard"
import { DietPillGroup } from "@/components/DietPillGroup"
import { TagInput } from "@/components/TagInput"
import { Loader2 } from "lucide-react"
import { UrlDetectionBadge } from "@/components/UrlDetectionBadge"
import { ServingStepper } from "@/components/ServingStepper"
import { ComparisonLayout } from "@/components/ComparisonLayout"
import { ExportToolbar } from "@/components/ExportToolbar"
```

<!-- App.tsx current h1 (line 90): -->
```tsx
<h1 className="text-2xl font-bold text-center mb-8">DietCode</h1>
```

<!-- App.tsx current main element (line 89): -->
```tsx
<main className="px-4 py-8">
```

<!-- Path alias: @/assets → frontend/src/assets/ (from components.json) -->
<!-- SVG authoring: viewBox="0 0 40 40", no external refs, static only -->
<!-- emerald-700 hex: #2d7a4f | amber-400 hex: #f59e0b | slate-300 hex: #cbd5e1 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dietcode-logo.svg (kale leaf + fruit + robot)</name>
  <files>frontend/src/assets/dietcode-logo.svg</files>
  <read_first>
    - /Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-CONTEXT.md — D-04/D-05 design brief: kale leaf (green/textured) + colorful fruit (apple or orange) + small robot face (silver/warm gray, friendly). Playful cartoon-ish. Works at 32–40px.
    - /Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-PATTERNS.md — SVG authoring conventions section (viewBox, colors, no JS, no animation)
  </read_first>
  <action>
Create the file `frontend/src/assets/dietcode-logo.svg`. The SVG must be:
- `viewBox="0 0 40 40"` (40×40 design units, renders at h-9 w-9 = 36px in the header)
- Static, no animation, no JS, fully self-contained
- Playful cartoon-ish style — not a corporate icon
- Three distinct elements: kale leaf (left/top), colorful fruit (right/top), robot face (center/bottom)

Color palette to use literally (no CSS variables in SVG — the SVG is used as <img> so CSS variables don't apply):
- Kale green: `#2d7a4f` (emerald-700 = warm primary)
- Kale highlight: `#4ade80` (lighter green for leaf veins/texture)
- Fruit: `#f59e0b` (amber-400 = warm orange, represents an apple or orange)
- Fruit highlight: `#fde68a` (amber-100, lighter spot on fruit)
- Robot body: `#94a3b8` (slate-400, friendly gray)
- Robot face: `#e2e8f0` (slate-200, lighter screen/face)
- Robot eyes: `#1e293b` (slate-900, two small dot eyes)
- Robot accent: `#2d7a4f` (emerald, small smile line or antenna tip)

Suggested layout within the 40×40 viewbox:
- Kale leaf: SVG path or ellipse at upper-left quadrant (x:2–18, y:2–20). A simple stylized kale shape: an oval with a scalloped/wavy edge and a center vein line.
- Fruit (apple/orange): Circle at upper-right quadrant (cx:28, cy:12, r:8). Add a small stem (rectangle, 1×3, dark brown) at the top center of the circle.
- Robot face: Rounded rectangle at center-bottom (x:10, y:22, width:20, height:16, rx:3). Two dot eyes (circles r:2 at y:27). A straight line mouth or tiny arc.

Keep shapes simple — the logo renders at 36px; fine details disappear. Aim for 20–35 SVG elements total.

The SVG root element must have `xmlns="http://www.w3.org/2000/svg"` and `role="img"` is NOT needed (the <img> tag in App.tsx will have aria-hidden="true").
  </action>
  <verify>
    <automated>grep -c "viewBox" /Users/mariabodoh/repos/DietCode/frontend/src/assets/dietcode-logo.svg && grep "viewBox=\"0 0 40 40\"" /Users/mariabodoh/repos/DietCode/frontend/src/assets/dietcode-logo.svg</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/assets/dietcode-logo.svg
    - File contains `viewBox="0 0 40 40"`
    - File contains `xmlns="http://www.w3.org/2000/svg"`
    - File contains at least one path or circle element using fill color `#2d7a4f` (kale green)
    - File contains at least one circle or path element using fill color `#f59e0b` (fruit amber)
    - File contains at least one rect or path element using fill color `#94a3b8` (robot gray)
    - File contains no `<script>` tags
    - File contains no `<animate>` or `<animateTransform>` tags
  </acceptance_criteria>
  <done>dietcode-logo.svg exists with three visual elements (kale leaf, fruit, robot face) using the warm palette, static only, viewBox 0 0 40 40.</done>
</task>

<task type="auto">
  <name>Task 2: Add colored header band to App.tsx (D-02, D-05)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx — read the full file before editing; locate the current <h1> at line ~90 and the existing import block at lines 1–9. Understand the full JSX return structure before modifying.
  </read_first>
  <action>
Make two changes to frontend/src/App.tsx:

**Change 1 — Add logo import** (after the existing import block, as the last import line):
```tsx
import dietcodeLogo from "@/assets/dietcode-logo.svg"
```

**Change 2 — Replace the existing `<h1>` with a full-bleed `<header>` band** placed BEFORE the `<main>` element in the JSX return.

Current JSX structure to find:
```tsx
return (
  <div ...>
    <main className="px-4 py-8">
      ...
      <h1 className="text-2xl font-bold text-center mb-8">DietCode</h1>
      ...
```

The `<h1>` at line ~90 is REMOVED entirely. In its place, insert a `<header>` element as a sibling of `<main>`, positioned BEFORE it in the JSX. The updated return structure:

```tsx
return (
  <div ...>
    <header className="w-full bg-emerald-700 text-white py-4 px-4 sm:px-6 lg:px-8 flex items-center gap-3">
      <img
        src={dietcodeLogo}
        alt=""
        aria-hidden="true"
        className="h-9 w-9 flex-shrink-0"
      />
      <h1 className="text-2xl font-bold text-white leading-none">DietCode</h1>
    </header>
    <main className="px-4 py-8">
      {/* existing main content — h1 removed from here */}
      ...
```

Notes:
- The `<h1>` MOVES into the `<header>` element — it is NOT deleted from the document, just relocated. This preserves page heading semantics.
- `bg-emerald-700` is the Tailwind utility matching `--primary` ~emerald-700. Using the Tailwind utility directly on the header (not bg-primary) keeps the header color independent from the CSS variable for simplicity and predictability.
- `text-white` is applied both on the `<header>` element and explicitly on `<h1>` to ensure the wordmark renders white.
- `flex-shrink-0` on the image prevents the logo from squishing at narrow widths.
- Do NOT change any other content inside `<main>`.
- Do NOT change the outer `<div>` wrapper or any other structural elements.
- Preserve all existing imports, state, handlers, and content in the file.
  </action>
  <verify>
    <automated>grep -n "dietcode-logo.svg\|bg-emerald-700\|aria-hidden" /Users/mariabodoh/repos/DietCode/frontend/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `import dietcodeLogo from "@/assets/dietcode-logo.svg"`
    - App.tsx contains `bg-emerald-700` in a `<header>` element
    - App.tsx contains `aria-hidden="true"` on the logo img tag
    - App.tsx contains `<h1` inside the `<header>` element (h1 relocated, not deleted)
    - App.tsx does NOT contain `<h1 className="text-2xl font-bold text-center mb-8">DietCode</h1>` (original standalone h1 removed)
    - App.tsx still contains all original imports (useState, RecipeCard, DietPillGroup, etc.)
    - `cd /Users/mariabodoh/repos/DietCode/frontend && npm run build` succeeds with no errors
  </acceptance_criteria>
  <done>App.tsx has the logo import and a full-bleed emerald header band with the logo image and DietCode h1 wordmark, before the existing main element.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG asset | Static SVG file loaded as <img> with aria-hidden; no script execution path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | dietcode-logo.svg | accept | Static SVG asset used as <img> src; browser treats as image, does not execute script. No script tags in SVG. |
| T-05-03 | — | App.tsx header band | accept | Pure presentational change; no user input, no new data flows, no auth. |
</threat_model>

<verification>
```bash
cd /Users/mariabodoh/repos/DietCode/frontend && npm run build 2>&1 | tail -10
```

Build must succeed. Vite resolves `@/assets/dietcode-logo.svg` through the path alias configured in components.json / vite.config.ts.
</verification>

<success_criteria>
1. `frontend/src/assets/dietcode-logo.svg` exists and contains viewBox="0 0 40 40" with three visual elements.
2. `frontend/src/App.tsx` contains `import dietcodeLogo` and a `<header className="... bg-emerald-700 ...">` element before `<main>`.
3. `npm run build` in /frontend succeeds with no errors.
</success_criteria>

<output>
After completion, create `/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-02-SUMMARY.md`
</output>
