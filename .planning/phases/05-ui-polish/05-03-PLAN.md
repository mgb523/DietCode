---
phase: 05-ui-polish
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/components/DietPillGroup.tsx
autonomous: true
requirements: [UI-POLISH-03]

must_haves:
  truths:
    - "Clicking the Info icon next to a diet pill label opens a Popover (not a tooltip) with solid white opaque background"
    - "The Popover closes when clicking outside — no close button needed"
    - "All diet pill buttons show a visible focus ring when navigated via keyboard (Tab key)"
    - "Selected diet pills render with solid warm-green fill (bg-primary) and white text — passes the glance test"
  artifacts:
    - path: "frontend/src/components/DietPillGroup.tsx"
      provides: "Diet pills with Popover info icons and focus-visible rings"
      contains: "PopoverContent"
  key_links:
    - from: "DietPillGroup.tsx pill button"
      to: "@/components/ui/popover"
      via: "PopoverTrigger asChild wrapping button"
      pattern: "PopoverContent"
---

<objective>
Switch DietPillGroup.tsx info icons from hover-based Tooltip to click-based Popover (D-07), and add focus-visible ring classes to pill buttons (D-13). The Popover is already installed (shadcn ui/popover.tsx) — this is a component swap within a single file. The selected pill fill already uses bg-primary class strings (D-06); once D-03 changes --primary to emerald-green via index.css, pills automatically look correct with no class changes.

Purpose: Fixes two UX bugs — transparent/clashing tooltip backgrounds on desktop and non-functional hover tooltips on mobile (touch screens have no hover). D-07, D-13 (pills portion) from CONTEXT.md.

Output: Updated DietPillGroup.tsx with Popover-based info icons and focus-visible rings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mariabodoh/repos/DietCode/.planning/ROADMAP.md
@/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-CONTEXT.md
@/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-PATTERNS.md

<interfaces>
<!-- DietPillGroup.tsx current imports (lines 1–3): -->
```tsx
import { cn } from "@/lib/utils"
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip"
import { Info } from "lucide-react"
```

<!-- DietPillGroup.tsx current pill button cn() block (lines 64–69): -->
```tsx
className={cn(
  "flex items-center gap-1.5 px-3 py-1 rounded-full text-sm font-normal border cursor-pointer transition-colors",
  selected.includes(opt.value)
    ? "bg-primary text-primary-foreground border-primary"
    : "bg-background text-foreground border-border hover:bg-muted"
)}
```

<!-- DietPillGroup.tsx current tooltip block (lines 72–87): -->
```tsx
<Tooltip>
  <TooltipTrigger asChild>
    <span
      role="img"
      aria-label={`Info about ${opt.label}`}
      onClick={e => e.stopPropagation()}
      className="inline-flex items-center"
    >
      <Info className="h-3 w-3 opacity-60" />
    </span>
  </TooltipTrigger>
  <TooltipContent side="bottom" className="max-w-[220px] text-xs">
    {opt.description}
  </TooltipContent>
</Tooltip>
```

<!-- SubstitutionPopover.tsx Popover pattern to copy (reference only, do not modify): -->
```tsx
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"
// Pattern:
<Popover>
  <PopoverTrigger asChild>
    <button type="button" aria-label={...} className="...cursor-pointer inline-flex items-center...">
      <Info className="h-4 w-4" />
    </button>
  </PopoverTrigger>
  <PopoverContent className="max-w-[280px] text-sm">
    {/* content */}
  </PopoverContent>
</Popover>
```

<!-- ui/popover.tsx is installed at frontend/src/components/ui/popover.tsx — no install needed -->
<!-- cn() from @/lib/utils — already imported in DietPillGroup -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tooltip with Popover + add focus-visible rings in DietPillGroup.tsx</name>
  <files>frontend/src/components/DietPillGroup.tsx</files>
  <read_first>
    - frontend/src/components/DietPillGroup.tsx — read the full file before editing; understand all imports, the pill map structure, and the current Tooltip block location
    - frontend/src/components/SubstitutionPopover.tsx — read to confirm the Popover usage pattern (PopoverTrigger asChild + button child) — this is the exact pattern to replicate
    - frontend/src/components/ui/popover.tsx — verify exports are Popover, PopoverContent, PopoverTrigger (confirm import path before writing)
  </read_first>
  <action>
Make three targeted changes to frontend/src/components/DietPillGroup.tsx:

**Change 1 — Swap import line** (replace line 2 — the Tooltip import):

Remove:
```tsx
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip"
```

Replace with:
```tsx
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"
```

Keep the `import { cn } from "@/lib/utils"` and `import { Info } from "lucide-react"` lines unchanged.

If the file also imports `TooltipProvider` from tooltip (check before editing), remove that import too. Do not leave any unused Tooltip imports.

**Change 2 — Add focus-visible ring to pill button className** (inside the pill .map() block):

In the outer `<button>` element that wraps each pill, find the `className={cn(...)}` block. Add the focus-visible ring string as a second static line in the cn() call:

```tsx
className={cn(
  "flex items-center gap-1.5 px-3 py-1 rounded-full text-sm font-normal border cursor-pointer transition-colors",
  "focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring",
  selected.includes(opt.value)
    ? "bg-primary text-primary-foreground border-primary"
    : "bg-background text-foreground border-border hover:bg-muted"
)}
```

Do NOT change the selected/unselected conditional class strings — those are correct per D-06 (they reference bg-primary which already picked up the warm green from D-03/index.css).

**Change 3 — Replace Tooltip block with Popover block** (the info icon section):

Find the `<Tooltip>...</Tooltip>` block (currently wrapping a `<span>` trigger and `<TooltipContent>`). Replace entirely with:

```tsx
<Popover>
  <PopoverTrigger asChild>
    <button
      type="button"
      role="img"
      aria-label={`Info about ${opt.label}`}
      onClick={e => e.stopPropagation()}
      className="inline-flex items-center cursor-pointer"
    >
      <Info className="h-3 w-3 opacity-60" />
    </button>
  </PopoverTrigger>
  <PopoverContent side="bottom" className="max-w-[220px] text-xs">
    {opt.description}
  </PopoverContent>
</Popover>
```

Key differences from the old Tooltip block:
- `<Tooltip>` → `<Popover>`
- `<TooltipTrigger asChild>` → `<PopoverTrigger asChild>`
- `<span>` trigger → `<button type="button">` trigger (required for PopoverTrigger asChild)
- `<TooltipContent>` → `<PopoverContent>`
- PopoverContent renders in a Portal with solid `bg-popover` background — no transparency issues

If the file wraps pills in a `<TooltipProvider>` at the outer level, remove that wrapper too (Popover does not require a Provider).

After all three changes: run `npm run build` from /frontend to verify no TypeScript errors.
  </action>
  <verify>
    <automated>grep -n "PopoverContent\|TooltipContent\|focus-visible:ring" /Users/mariabodoh/repos/DietCode/frontend/src/components/DietPillGroup.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DietPillGroup.tsx contains `PopoverContent` (Popover is being used)
    - DietPillGroup.tsx does NOT contain `TooltipContent` (Tooltip fully removed)
    - DietPillGroup.tsx does NOT contain `TooltipTrigger` (Tooltip fully removed)
    - DietPillGroup.tsx contains `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring` in the pill button className
    - DietPillGroup.tsx contains `import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"`
    - DietPillGroup.tsx does NOT contain `import { Tooltip` (old import gone)
    - DietPillGroup.tsx still contains `bg-primary text-primary-foreground border-primary` for the selected pill state
    - `cd /Users/mariabodoh/repos/DietCode/frontend && npm run build` succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>DietPillGroup.tsx uses Popover (not Tooltip) for info icons, pill buttons have focus-visible ring classes, and selected pill class strings are unchanged (they inherit warm green from index.css D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popover content | Diet descriptions are static strings from the dietOptions constant array — no user input rendered in popover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | XSS | PopoverContent rendering opt.description | accept | opt.description is a static constant defined in the same file; no user-supplied data. React JSX escapes strings by default. |
| T-05-05 | — | Tooltip removal | accept | Removing hover-based Tooltip does not introduce new attack surface; Popover is same library (Radix UI via shadcn). |
</threat_model>

<verification>
```bash
cd /Users/mariabodoh/repos/DietCode/frontend && npm run build 2>&1 | grep -E "error|warning|ERROR" | head -10
```

No TypeScript errors. If `TooltipProvider` was wrapping pills, its removal must not break other components (it only wraps the pills map, not the whole app).
</verification>

<success_criteria>
1. DietPillGroup.tsx imports Popover, PopoverContent, PopoverTrigger from ui/popover — no Tooltip imports remain.
2. Each pill's outer button has `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring` in its className.
3. The info icon is wrapped in Popover + PopoverTrigger asChild with a button trigger, not a span.
4. `npm run build` succeeds.
</success_criteria>

<output>
After completion, create `/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-03-SUMMARY.md`
</output>
