---
phase: 05-ui-polish
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - frontend/src/components/RecipeCard.tsx
  - frontend/src/components/ServingStepper.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [UI-POLISH-04]

must_haves:
  truths:
    - "RecipeCard 'Ingredients' and 'Instructions' section headings are bold (font-bold), not semibold"
    - "Instruction list items have relaxed line-height (leading-relaxed) for improved readability"
    - "The transform error paragraph has role='alert' so screen readers announce it immediately"
    - "The empty-state paragraph reads 'Original recipe unavailable — paste it above to see the comparison.' (full copy)"
    - "The main element has responsive padding: px-4 sm:px-6 lg:px-8 py-8"
    - "The textarea uses focus-visible: (not focus:) for its outline and ring classes"
    - "ServingStepper +/- buttons have focus-visible ring classes for keyboard navigation"
  artifacts:
    - path: "frontend/src/components/RecipeCard.tsx"
      provides: "font-bold headings, leading-relaxed instruction items"
      contains: "font-bold mb-2"
    - path: "frontend/src/components/ServingStepper.tsx"
      provides: "+/- buttons with focus-visible ring"
      contains: "focus-visible:ring-1"
    - path: "frontend/src/App.tsx"
      provides: "responsive main padding, role=alert, corrected empty-state copy, focus-visible textarea"
      contains: "role=\"alert\""
  key_links:
    - from: "App.tsx error paragraph"
      to: "Screen reader announcement"
      via: "role='alert' attribute"
      pattern: "role=\"alert\""
---

<objective>
Apply the seven mechanical fixes from CONTEXT.md D-08 through D-13 across three files: RecipeCard.tsx (typography), ServingStepper.tsx (focus rings), and App.tsx (responsive padding, accessibility, copy, focus-visible textarea). These are surgical line-level edits — no structural changes, no new components, no new imports.

Purpose: Addresses typography inconsistencies, accessibility gaps, and responsive layout issues identified in the Phase 5 UI audit. D-08, D-09, D-10, D-11, D-12, D-13 (stepper and textarea portions) from CONTEXT.md.

Output: Three files updated with surgical line edits. App.tsx depends on 05-02 completing first (header band restructure must be in the file before these edits are applied).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mariabodoh/repos/DietCode/.planning/ROADMAP.md
@/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-CONTEXT.md
@/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-PATTERNS.md

<interfaces>
<!-- RecipeCard.tsx target lines from PATTERNS.md: -->
<!-- Line ~140: <h3 className="font-semibold mb-2">Ingredients</h3>  →  font-bold -->
<!-- Line ~167: <h3 className="font-semibold mb-2">Instructions</h3> →  font-bold -->
<!-- Line ~169: <li key={i}>{step}</li>  →  <li key={i} className="leading-relaxed">{step}</li> -->

<!-- ServingStepper.tsx target from PATTERNS.md: -->
<!-- Decrement button cn() — add "focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring" -->
<!-- Increment button cn() — add "focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring" -->

<!-- App.tsx target lines from PATTERNS.md (AFTER 05-02 header band change): -->
<!-- main element: className="px-4 py-8"  →  "px-4 sm:px-6 lg:px-8 py-8"  (D-08) -->
<!-- textarea className: focus:outline-none focus:ring-1 focus:ring-ring  →  focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring  (D-13) -->
<!-- error <p>: <p className="text-sm text-destructive mt-2">  →  <p role="alert" className="text-sm text-destructive mt-2">  (D-11) -->
<!-- empty state: "Original recipe unavailable."  →  "Original recipe unavailable — paste it above to see the comparison."  (D-12) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RecipeCard.tsx typography (D-09, D-10)</name>
  <files>frontend/src/components/RecipeCard.tsx</files>
  <read_first>
    - frontend/src/components/RecipeCard.tsx — read the full file before editing; locate both h3 tags and the instruction ol/li block. Confirm exact current class strings before replacing.
  </read_first>
  <action>
Make three surgical changes to frontend/src/components/RecipeCard.tsx:

**Change 1 — Ingredients h3 (D-09):**
Find:
```tsx
<h3 className="font-semibold mb-2">Ingredients</h3>
```
Replace with:
```tsx
<h3 className="font-bold mb-2">Ingredients</h3>
```

**Change 2 — Instructions h3 (D-09):**
Find:
```tsx
<h3 className="font-semibold mb-2">Instructions</h3>
```
Replace with:
```tsx
<h3 className="font-bold mb-2">Instructions</h3>
```

**Change 3 — Instruction list item leading-relaxed (D-10):**
Find the instruction list items. Current pattern:
```tsx
<li key={i}>{step}</li>
```
Replace with:
```tsx
<li key={i} className="leading-relaxed">{step}</li>
```

If the `<li>` already has a className, append `leading-relaxed` to the existing class string rather than replacing it.

Do NOT change any other class strings, imports, props, or logic in this file. All other content is correct per 05-PATTERNS.md.
  </action>
  <verify>
    <automated>grep -n "font-bold mb-2\|leading-relaxed\|font-semibold mb-2" /Users/mariabodoh/repos/DietCode/frontend/src/components/RecipeCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RecipeCard.tsx contains `font-bold mb-2` (at least twice — Ingredients and Instructions h3 headings)
    - RecipeCard.tsx does NOT contain `font-semibold mb-2` (both instances replaced)
    - RecipeCard.tsx contains `className="leading-relaxed"` on a `<li>` element inside the instructions ol
    - No other lines in RecipeCard.tsx were changed (verify line count is same ± 0 net lines if edits are in-place replacements)
  </acceptance_criteria>
  <done>RecipeCard.tsx h3 headings use font-bold (not font-semibold), and instruction list items have leading-relaxed class.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ServingStepper.tsx focus rings + App.tsx mechanical fixes (D-08, D-11, D-12, D-13)</name>
  <files>frontend/src/components/ServingStepper.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/ServingStepper.tsx — read full file before editing; locate both +/- button cn() blocks. Confirm there are exactly two button elements (decrement and increment).
    - frontend/src/App.tsx — read full file before editing; locate: (a) the main element className, (b) the textarea className with focus: classes, (c) the error paragraph, (d) the "Original recipe unavailable." paragraph. The header band from 05-02 will already be in this file — do not disturb it.
  </read_first>
  <action>
**ServingStepper.tsx changes (D-13):**

Add `"focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring"` as an additional string argument to the `cn()` call on BOTH the decrement and increment buttons.

Decrement button — find the current cn() block:
```tsx
className={cn(
  "min-w-9 h-9 flex items-center justify-center",
  "border border-border bg-background hover:bg-muted",
  "rounded-l-md transition-colors",
  "disabled:opacity-40 disabled:cursor-not-allowed"
)}
```
Replace with:
```tsx
className={cn(
  "min-w-9 h-9 flex items-center justify-center",
  "border border-border bg-background hover:bg-muted",
  "rounded-l-md transition-colors",
  "disabled:opacity-40 disabled:cursor-not-allowed",
  "focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring"
)}
```

Increment button — find the current cn() block:
```tsx
className={cn(
  "min-w-9 h-9 flex items-center justify-center",
  "border border-border bg-background hover:bg-muted",
  "rounded-r-md transition-colors"
)}
```
Replace with:
```tsx
className={cn(
  "min-w-9 h-9 flex items-center justify-center",
  "border border-border bg-background hover:bg-muted",
  "rounded-r-md transition-colors",
  "focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring"
)}
```

Do NOT change any other content in ServingStepper.tsx.

---

**App.tsx changes (D-08, D-11, D-12, D-13) — apply after 05-02 has already restructured the header:**

**Change A — Responsive main padding (D-08):**
Find:
```tsx
<main className="px-4 py-8">
```
Replace with:
```tsx
<main className="px-4 sm:px-6 lg:px-8 py-8">
```

**Change B — Textarea focus-visible (D-13):**
In the textarea className string, find the `focus:` pattern (there should be two instances: `focus:outline-none` and `focus:ring-1`):
```
focus:outline-none focus:ring-1 focus:ring-ring
```
Replace with:
```
focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring
```

**Change C — Error paragraph role="alert" (D-11):**
Find:
```tsx
<p className="text-sm text-destructive mt-2">⚠ {error}</p>
```
Replace with:
```tsx
<p role="alert" className="text-sm text-destructive mt-2">⚠ {error}</p>
```

**Change D — Empty state copy (D-12):**
Find:
```tsx
Original recipe unavailable.
```
(Inside a `<p className="text-sm text-muted-foreground">` element)
Replace text content with:
```
Original recipe unavailable — paste it above to see the comparison.
```
Use an em dash (—, U+2014), not a hyphen-minus (-).

Do NOT change anything else in App.tsx. The header band from 05-02 must remain intact.
  </action>
  <verify>
    <automated>grep -n "focus-visible:ring-1\|role=\"alert\"\|paste it above\|sm:px-6" /Users/mariabodoh/repos/DietCode/frontend/src/components/ServingStepper.tsx /Users/mariabodoh/repos/DietCode/frontend/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ServingStepper.tsx contains `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring` (appears twice — one for each button)
    - App.tsx contains `sm:px-6 lg:px-8` in the main element className (D-08)
    - App.tsx does NOT contain `focus:outline-none focus:ring-1` in the textarea className (replaced with focus-visible:)
    - App.tsx contains `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring` in the textarea className (D-13)
    - App.tsx contains `role="alert"` on the error paragraph (D-11)
    - App.tsx contains `paste it above to see the comparison` in the empty state paragraph (D-12)
    - App.tsx still contains `bg-emerald-700` in the header band (05-02 change preserved)
    - `cd /Users/mariabodoh/repos/DietCode/frontend && npm run build` succeeds
  </acceptance_criteria>
  <done>ServingStepper +/- buttons have focus-visible rings; App.tsx has responsive main padding, focus-visible textarea, role=alert error paragraph, and corrected empty-state copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role="alert" | ARIA live region — screen readers will announce the error message content when it appears |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | role="alert" error paragraph | accept | Error messages are already visible on screen; role="alert" only adds screen reader announcement. Error content is controlled by the app (not user-supplied). |
| T-05-07 | — | ServingStepper focus ring | accept | CSS-only change; no new behavior or data flow. |
| T-05-08 | — | Responsive padding | accept | CSS-only change; no new behavior or data flow. |
</threat_model>

<verification>
```bash
cd /Users/mariabodoh/repos/DietCode/frontend && npm run build 2>&1 | tail -5
```

All three files must compile without TypeScript errors.

Spot-check key strings:
```bash
grep -c "focus-visible:ring-1" /Users/mariabodoh/repos/DietCode/frontend/src/components/ServingStepper.tsx
# Expected: 2 (one per button)

grep "role=\"alert\"" /Users/mariabodoh/repos/DietCode/frontend/src/App.tsx
# Expected: at least one match
```
</verification>

<success_criteria>
1. RecipeCard.tsx: `font-semibold mb-2` no longer exists; `font-bold mb-2` appears twice; instruction li has `leading-relaxed`.
2. ServingStepper.tsx: `focus-visible:ring-1` appears exactly twice (both buttons).
3. App.tsx: main has `sm:px-6 lg:px-8`; textarea has `focus-visible:`; error p has `role="alert"`; empty state has full copy with em dash.
4. `npm run build` succeeds in /frontend.
</success_criteria>

<output>
After completion, create `/Users/mariabodoh/repos/DietCode/.planning/phases/05-ui-polish/05-04-SUMMARY.md`
</output>
